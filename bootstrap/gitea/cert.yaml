apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitea-prod
  namespace: git
spec:
  secretName: gitea-prod-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - git.homelab.platformfeverdream.io